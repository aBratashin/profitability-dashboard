import {cva} from 'class-variance-authority';

export const cvaButtonContainer = cva(
    [
        'px-3 py-1 rounded-2xl font-medium',
    ],
);

export const cvaWhite = cva(
    [
        'px-3 py-1 rounded-2xl font-medium',
    ],
);

export const cvaTransparent = cva(
    [
        'px-3 py-1 rounded-2xl font-medium',
    ],
);
