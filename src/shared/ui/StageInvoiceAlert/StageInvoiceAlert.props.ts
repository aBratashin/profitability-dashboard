import {DetailedHTMLProps, HTMLAttributes, ReactNode} from 'react';

export interface StageInvoiceAlertProps extends DetailedHTMLProps<HTMLAttributes<HTMLDivElement>, HTMLDivElement> {
    children: ReactNode;
}